import 'package:flutter/material.dart';

class ClubFixturesTab extends StatelessWidget {
  const ClubFixturesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}