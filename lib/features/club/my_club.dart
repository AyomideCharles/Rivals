import 'package:flutter/material.dart';

class MyClub extends StatelessWidget {
  const MyClub({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('My Club')));
  }
}
